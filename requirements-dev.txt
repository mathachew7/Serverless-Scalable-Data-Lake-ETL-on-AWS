# Dev / test dependencies — NOT deployed to Lambda or Glue
pytest>=8.0
pytest-cov>=5.0
boto3>=1.34
moto[s3,glue]>=5.0
pyspark>=3.5.0    # local Spark for testing glue/transforms.py (requires Java 11+)
